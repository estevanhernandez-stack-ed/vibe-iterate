# Social posts — Vibe Plugins in the Claude Code directory

Copy-paste ready. Accepted/published today: **vibe-cartographer, vibe-doc, vibe-keystone**. The rest (iterate, test, thesis, thesis-engine, taker) are submitted/pending — bump the named list as more get accepted.

---

## LinkedIn

```
Three of our Vibe Plugins are now live in the Claude Code plugin directory.

626Labs builds Claude Code plugins for people who vibe-code fast and still ship for real. Live in the directory now:

— vibe-cartographer: idea to shipped app, spec-first, with memory that sharpens every run.
— vibe-doc: finds your documentation gaps and writes the missing docs from your own code.
— vibe-keystone: bootstraps a CLAUDE.md any agent can stand on, tuned to your repo.

The rest of the family is right behind them: vibe-iterate (post-ship iteration), vibe-test (tests that actually matter), vibe-taker (move a feature between repos), plus our research tooling.

Each one has its own page now: https://626labs.dev/plugins

Built in the open, benchmarked against the same maturity ladder, no telemetry. Imagine Something Else.
```

---

## X

```
Our Vibe Plugins are in the Claude Code plugin directory.

vibe-cartographer, vibe-doc & vibe-keystone — live now. vibe-iterate, vibe-test & vibe-taker right behind.

Idea → shipped → maintained, all in Claude Code.

See the family: https://626labs.dev/plugins
```

(~245 characters — fits the standard limit.)

---

## X — alternate (single-plugin spotlight, reuse as each gets accepted)

```
vibe-<name> is now in the Claude Code plugin directory.

<one-line what it does>

Install: /plugin install vibe-<name>@vibe-plugins
Page: https://626labs.dev/vibe-<name>/
```

---

## Notes
- **Links** — `https://626labs.dev/plugins` is the family index; each plugin also has `https://626labs.dev/<plugin>/`.
- **Voice** — clean, no emoji (626Labs brand). If you want an emoji-forward X variant for reach, say so.
- **Accuracy** — only name plugins actually in the directory as "live." Move names from "right behind" to "live now" as they're accepted.
